### Embedded CLI — STM32F446RE

UART command-line interface for peripheral control on STM32F446RE. Commands include LED blinking, software and hardware PWM dimming, and ADC temperature readout. Bare-metal branch uses no FreeRTOS.

